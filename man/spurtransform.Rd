% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spur_formula_api.R
\name{spurtransform}
\alias{spurtransform}
\title{Transform Variables Using SPUR Spatial Transformations}
\usage{
spurtransform(
  formula,
  data,
  prefix = "h_",
  transformation = "lbmgls",
  radius = NULL,
  clustvar = NULL,
  lon = NULL,
  lat = NULL,
  coords_euclidean = NULL,
  separately = FALSE,
  replace = FALSE,
  verbose = FALSE
)
}
\arguments{
\item{formula}{Formula listing variables to transform. All variables
in the formula are transformed. Both one-sided (\code{~ y + x1 + x2})
and two-sided (\code{y ~ x1 + x2}) formulas work, so you can pass your
analysis formula directly.}

\item{data}{A data frame containing variables and coordinates.}

\item{prefix}{Prefix for transformed variable names.}

\item{transformation}{One of \code{"lbmgls"}, \code{"nn"}, \code{"iso"}, or \code{"cluster"}.}

\item{radius}{Radius used by \code{transformation = "iso"}.}

\item{clustvar}{Cluster variable used by \code{transformation = "cluster"}.}

\item{lon}{Optional longitude column name (must be supplied with \code{lat}).}

\item{lat}{Optional latitude column name (must be supplied with \code{lon}).}

\item{coords_euclidean}{Optional Euclidean coordinate column names.}

\item{separately}{Logical; handle missingness separately by variable.}

\item{replace}{Logical; allow overwriting existing output columns.}

\item{verbose}{Logical; if \code{TRUE}, print coordinate diagnostics.}
}
\value{
A data frame with transformed variables appended.
}
\description{
Formula interface for \code{spurtransform}.
}
\examples{
data(spur_example)

# One-sided formula: transform listed variables
out <- spurtransform(~ am + rm, data = spur_example,
                     lon = "lon", lat = "lat", transformation = "lbmgls")
head(out[, c("am", "h_am", "rm", "h_rm")])

# Two-sided formula: pass your analysis formula to transform all variables
out2 <- spurtransform(am ~ gini + fracblack, data = spur_example,
                      lon = "lon", lat = "lat", transformation = "lbmgls")
head(out2[, c("h_am", "h_gini", "h_fracblack")])
}
