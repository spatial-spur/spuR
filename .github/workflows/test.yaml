name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup R
        uses: r-lib/actions/setup-r@v2

      - name: Install R dependencies
        uses: r-lib/actions/setup-r-dependencies@v2
        with:
          # TODO: consider moving geodist from scpcR Suggests to Imports, since geographic scpc() calls require it at runtime.
          extra-packages: any::testthat, any::geodist

      - name: Run tests
        run: Rscript -e 'testthat::test_local(stop_on_failure = TRUE)'
